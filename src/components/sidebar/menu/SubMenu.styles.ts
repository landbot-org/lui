import { styled } from 'styled-components';

import { TooltipContent } from '../../tooltip';

export const StyledContent = styled(TooltipContent)`
  z-index: 1011;
`;

export const StyledSubMenuContent = styled.ul`
  margin: 0;
  padding: 0;
`;
